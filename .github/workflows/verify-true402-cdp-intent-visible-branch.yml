name: Verify true402 CDP Intent Visibility

on:
  push:

permissions:
  contents: read

jobs:
  audit:
    runs-on: ubuntu-slim
    timeout-minutes: 3
    steps:
      - name: Verify buyer searches
        shell: bash
        run: |
          set -euo pipefail
          for q in 'x402 settlement test' 'CDP settlement canary' 'wallet facilitator test'; do
            echo "=== $q ==="
            curl -sS --get --connect-timeout 5 --max-time 20               'https://true402.dev/api/v1/services'               --data-urlencode "q=$q"               --data-urlencode 'limit=20'               | jq -c '{total,rows:[(.data//[])[]|select((.url//"")|contains("agentresolver.vercel.app"))|{id,url,name:(.manifest.name//null),price:(.manifest.pricing.base//null)}]}'
          done
